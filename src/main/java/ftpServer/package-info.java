/**
 * This package contains classes that define an FTP server and manages
 * connections with FTP clients.
 * <p>
 * It contains an interface and two classes. The {@code Main} class
 * contains the driver method main that starts the server. The other class
 * {@code ClientHandler} defines a server session and implements the various
 * methods needed to communicate with the clients. Finally, the interface
 * {@code Server} defines the contract for an FTP server that interacts with an
 * FTP client
 */
package ftpServer;